import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PassDriverPage } from './pass-driver';

@NgModule({
  declarations: [
    PassDriverPage,
  ],
  imports: [
    IonicPageModule.forChild(PassDriverPage),
  ],
})
export class PassDriverPageModule {}
